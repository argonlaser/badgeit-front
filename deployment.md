To deploy the front service in now server, give the following command:
```
  now alias
```
For development use gulp dev

For production use, use gulp prod

You will get the latest changes in ```badgeit-front.now.sh```.
